<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suitecare.suitecare.api.mate.mapper.MateMapper">

    <select id="findMateById" resultType="MateDTO">
        SELECT mem.name, mem.gender, mem.birthday, mate.profile_picture_filename, mate.introduction, mate.contact_time_start, mate.contact_time_end
        FROM suitemember AS mem JOIN suitemate AS mate ON mem.id = mate.family_id
        WHERE mem.id = #{id};
    </select>

    <select id="getMateprofileById">
        SELECT id FROM suitemateprofile WHERE family_id = #{id}
    </select>

</mapper>