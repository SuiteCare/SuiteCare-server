<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suitecare.suitecare.api.mate.mapper.CareerMapper">

    <select id="findCareerById" resultType="CareerDTO">
        SELECT career.id, name, job_name, date_start, date_end
        FROM career
        JOIN materesume m ON m.id = mate_resume_id
        WHERE m.member_id =#{id};
    </select>

    <insert id="createCareer">
        INSERT INTO career(mate_resume_id, name, job_name, date_start, date_end)
        VALUES
        <foreach collection="career" item="item" separator=", ">
            ((SELECT id FROM materesume WHERE member_id = #{id}), #{item.name}, #{item.job_name}, #{item.date_start}, #{item.date_end})
        </foreach>
    </insert>


</mapper>