<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suitecare.suitecare.api.mate.mapper.MainServiceMapper">

    <select id="findMainServiceById" resultType="MainServiceDTO">
        SELECT mainservice.id, name
        FROM mainservice JOIN materesume ON mainservice.mate_resume_id = materesume.id
        WHERE materesume.member_id = #{id};
    </select>


</mapper>