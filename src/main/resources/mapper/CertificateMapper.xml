<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suitecare.suitecare.api.mate.mapper.CertificateMapper">

    <select id="findCertificateById" resultType="CertificateDTO">
        SELECT certificate.id, certificate_name, certificate_code, qualification_date, expired_date
        FROM certificate join materesume AS mate ON certificate.mate_resume_id = mate.id
        WHERE mate.member_id = #{id};
    </select>


</mapper>