<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suitecare.suitecare.api.mate.mapper.CertificateMapper">

    <select id="findCertificateById" resultType="CertificateDTO">
        SELECT c.id, name, code, qualification_date, expired_date
        FROM certificate c
        JOIN materesume m ON m.id = c.mate_resume_id
        WHERE m.member_id = #{id};
    </select>

    <insert id="createCertificate">
        INSERT INTO certificate(mate_resume_id, name, code, qualification_date, expired_date)
        VALUES
        <foreach collection="certificate" item="item" separator=", ">
            (#{resume_id}, #{item.name}, #{item.code}, #{item.qualification_date}, #{item.expired_date})
        </foreach>
    </insert>

</mapper>